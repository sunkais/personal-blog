---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title="关于我 | 松间笔记" description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main class="about-main">
			<section class="about-hero paper-card">
				<p class="eyebrow">About</p>
				<h1>关于孙开松</h1>
				<p>
					孙开松，数学硕士，关注偏微分方程、带奇异项的伪抛物方程、Galerkin 方法、变分法与 Sobolev 空间。
				</p>
				<p>
					这里不是一份简历，也不是一个很正式的学术主页。它更像一张慢慢铺开的书桌：论文、公式、工具、网页项目和一些生活里的小片段，会按照自己的节奏被整理到这里。
				</p>
			</section>

			<section class="about-grid" aria-label="关注的方向">
				<div>
					<h2>正在整理的东西</h2>
					<p>
						硕士论文相关的文献、证明细节和写作过程；偏微分方程里的奇异项、能量估计、Galerkin 近似和 Sobolev 空间工具；以及怎样把这些内容讲得更清楚。
					</p>
				</div>
				<div>
					<h2>也在实践的东西</h2>
					<p>
						AI 辅助学术写作、LaTeX 自动化、个人知识库和 Web 项目建设。Codex、Claude Code、GitHub Copilot 这些工具，会被当作书桌上的笔和纸，而不是替代思考的答案。
					</p>
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>
<style>
	.about-main {
		width: min(920px, calc(100% - 2rem));
	}

	.about-hero {
		padding: 2rem;
	}

	.about-hero h1 {
		font-family: Georgia, "Times New Roman", serif;
		font-size: 3.5rem;
	}

	.about-hero p:not(.eyebrow) {
		max-width: 42rem;
		color: var(--muted);
	}

	.about-grid {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 2rem;
		padding-top: 3rem;
	}

	.about-grid h2 {
		font-size: 1.35rem;
	}

	.about-grid p {
		color: var(--muted);
	}

	@media (max-width: 720px) {
		.about-main {
			width: min(100% - 1.25rem, 920px);
		}

		.about-hero {
			padding: 1.25rem;
		}

		.about-hero h1 {
			font-size: 2.6rem;
		}

		.about-grid {
			grid-template-columns: 1fr;
			gap: 1rem;
		}
	}
</style>
