---
import katex from 'katex';

interface Props {
	math: string;
	displayMode?: boolean;
}

const { math, displayMode = false } = Astro.props;

const html = katex.renderToString(math.trim(), {
	displayMode,
	throwOnError: false,
});
---

<Fragment set:html={html} />
